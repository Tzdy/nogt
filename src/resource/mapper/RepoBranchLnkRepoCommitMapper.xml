<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="RepoBranchLnkRepoCommitMapper">  

  <insert id="copyBranchLnk">
    insert into t_repo_branch_lnk_repo_commit(repo_branch_id, repo_commit_id) 
        select #{newBranchId} as repo_branch_id , repo_commit_id  from t_repo_branch_lnk_repo_commit  
        where repo_branch_id = #{oldBranchId}
  </insert>
</mapper>